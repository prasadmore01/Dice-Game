let roll = () =>{
    let random = Math.floor(Math.random());
    let diceNo = document.getElementById('dice');
    diceNo.innerText=(random);

    setTimeout(() => {
         rolled
    }, 1000);
}







